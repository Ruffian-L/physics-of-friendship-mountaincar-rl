# Pinned environment — verified on Python 3.11 (Linux), 2026-07-21.
# Direct dependencies only; pip resolves the rest.
gymnasium==1.3.0
numpy==2.4.6
matplotlib==3.11.1
ripser==0.6.15
persim==0.3.8
scikit-learn==1.9.0
networkx==3.6.1
